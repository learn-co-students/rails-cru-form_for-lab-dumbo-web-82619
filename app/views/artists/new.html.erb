<h1>Artist Sign-Up</h1>

<%= form_for @artist do |f| %>
  <%= f.label :name %><br>
  <%= f.text_field :name %><br>
  <%= f.label :bio %><br>
  <%= f.text_area :bio %><br>
  <%= f.submit 'Create Artist' %>
<% end %>